# Example environment variables for Hawx Recon Agent
# Copy this file to `.env` and fill in the values before running.

# Set your real LLM API key for LM Studio / OpenAI-compatible endpoint below
LLM_API_KEY=your_lm_studio_api_key_here

# LM Studio host and port (OpenAI-compatible API) — example/fake values
# Use a documentation/testnet address here to avoid exposing live information
LMSTUDIO_HOST=http://192.0.2.5:4321

# Preferred model to use (fake/example)
LLM_MODEL=qwen3-coder-30b-fake-model

# Optional overrides
# CONFIG_PATH=path/to/configs/config.yaml
# LAYER0_PATH=path/to/layer0.yaml

LLM_API_KEY=xxxx